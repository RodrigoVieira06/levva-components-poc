{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "lerna version && lerna publish --no-private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}