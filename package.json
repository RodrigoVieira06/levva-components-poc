{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "lerna version --no-private && lerna publish --force-publish"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}