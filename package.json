{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "lerna version --no-private && lerna publish from-package"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}